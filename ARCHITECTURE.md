# 🏗️ ChessWar Architecture & Guidelines

## 1. Architectural Background
본 프로젝트는 1년간의 개발 과정을 거치며 아키텍처의 근본적인 전환, 즉 **Soft Reset**을 단행했습니다. 
초기에는 엔터프라이즈 환경의 DDD 및 헥사고날 아키텍처를 채택하였으나, 마인크래프트의 물리적 틱 엔진 환경에서는 이러한 추상화가 과도한 매핑 오버헤드와 개발 속도 저하를 유발한다는 것을 확인했습니다.

또한 성능 최적화를 위한 DOD 및 순수 ECS 도입 역시 고려하였으나, Java 객체의 메모리 파편화 특성상 캐시 최적화의 이점을 얻기 어렵다고 판단하여 기각했습니다.

**[최종 채택 아키텍처]**
결론적으로 본 프로젝트는 기획 중심의 **GDD 유비쿼터스 언어**를 수용하고, 전통적 상속의 한계를 극복하기 위해 **Component-Based OOP** 패턴을 채택했습니다. 이는 엔진 환경에서 가장 빠르고 유연하게 MVP를 검증하기 위한 최적의 엔지니어링 결정입니다.

---

## 2. Core Patterns
본 프로젝트는 아래의 핵심 패턴을 기반으로 코드를 작성합니다.

*   **Engine-Native:** 외부 기술로부터 도메인을 격리하려는 시도를 지양합니다. `Player`, `Location`, `Entity` 등 Bukkit 엔진 객체를 로직 내 1급 객체로 직접 수용하여 복잡도를 낮춥니다.
*   **Component-Based OOP:** `Pawn -> MagicPawn`과 같은 깊은 상속 트리를 구성하지 않습니다. 깡통 엔티티에 `MoveComponent`, `AttackComponent` 등의 부품을 조립하여 동적으로 능력을 부여합니다.
*   **Manager & State Machine:** 데이터의 흐름은 복잡한 서비스 계층이 아닌 중앙 통제 방식으로 관리합니다.
    *   **State Machine:** 게임의 전체 흐름 통제
    *   **Manager:** 각 서브시스템 관리 및 엔진 이벤트와의 직접 연결

---

## 3. Practical Clean Code
게임 개발 특성에 맞춰 클린 코드를 실용적으로 적용합니다.

### 3.1. Naming Conventions
*   **패키지:** 단수형 소문자 사용 및 엔진 내장 객체와의 충돌 방지 (`player` ❌ → `participant` ⭕)
*   **클래스:** 역할에 따른 **기능적 접미사** 사용 강제 (`~Manager`, `~Component`, `~State`)
    *   의미 없는 찌꺼기 단어(`~Data`, `~Info`, `~Impl`) 사용은 절대 금지합니다.
*   **메서드:** `동사+명사` 형태로 영어 문장처럼 읽히게 작성하며, 과도한 줄임말을 금지합니다. (`calculateDamage` ⭕, `calcDmg` ❌)

### 3.2. Commenting Rules
빠른 이터레이션을 방해하는 불필요한 주석을 배제합니다.
1.  **Self-Documenting 최우선:** '무엇을' 하는지 설명하는 주석 대신 명확한 명명으로 의도를 전달합니다.
2.  **'Why'와 'TODO'만 허용:** 물리 연산 트릭이나 엔진 버그 회피 등 코드만으로 알 수 없는 **숨겨진 기술적 제약**만 주석으로 남깁니다.
3.  **JavaDoc 지연 작성:** 코어 API나 Manager를 제외한 일반 로직의 JavaDoc 작성은 아키텍처가 안정화되는 Post-MVP 단계까지 보류합니다.

---

## 4. The Priority Pyramid
설계 원칙과 현실이 충돌할 때 타협 없이 즉각적으로 의사결정을 내리기 위한 절대 기준입니다.

1.  **👑 1순위: MVP와 생존**
    *   오늘 당장 게임을 실행하여 재미를 검증할 수 있는 '동작하는 코드' 확보를 최우선으로 합니다.
2.  **🥈 2순위: 엔진의 법칙**
    *   클린 코드를 위한 구조화보다 마인크래프트 서버 틱 유지를 우선하며, 필요한 경우 `onTick` 루프 내에서의 절차적 하드코딩을 허용합니다.
3.  **🥉 3순위: GDD 기반 가독성**
    *   디자인 패턴 준수보다 기획 의도의 명확한 전달을 우선합니다. (파일 개수보다 이름의 의도 전달력이 중요)
4.  **🔨 4순위: 구현 패턴 준수**
    *   패턴은 개발을 돕는 도구일 뿐입니다. 오버엔지니어링이 예상되는 단순 로직에는 패턴을 강제하지 않습니다.

---
