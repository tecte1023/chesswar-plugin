# 🏗️ ChessWar Architecture & Guidelines

## 1. Architectural Background
본 프로젝트는 1년간의 개발 과정을 거치며 아키텍처의 근본적인 전환, 즉 **Soft Reset**을 단행했습니다. 
초기에는 엔터프라이즈 환경의 DDD 및 헥사고날 아키텍처를 채택하였으나, 마인크래프트의 물리적 틱 엔진 환경에서는 이러한 추상화가 과도한 매핑 오버헤드와 개발 속도 저하를 유발한다는 것을 확인했습니다.

또한 성능 최적화를 위한 DOD 및 순수 ECS 도입 역시 고려하였으나, Java 객체의 메모리 파편화 특성상 캐시 최적화의 이점을 얻기 어렵다고 판단하여 기각했습니다.

**[최종 채택 아키텍처]**
결론적으로 본 프로젝트는 기획 중심의 **GDD 유비쿼터스 언어**를 수용하고, 전통적 상속의 한계를 극복하기 위해 **Component-Based OOP** 패턴을 채택했습니다. 이는 엔진 환경에서 가장 빠르고 유연하게 MVP를 검증하기 위한 최적의 엔지니어링 결정입니다.

---

## 2. Core Patterns
본 프로젝트는 아래의 핵심 패턴을 기반으로 코드를 작성합니다.

### 2.1. Object Roles
모든 객체는 프로젝트 내 역할에 따라 아래 4가지 카테고리 중 하나로 명확히 분류됩니다. 이는 백엔드의 관성을 버리고 게임 개발에 최적화된 설계를 유지하기 위함입니다.

1. **순수 데이터 객체 (Struct / Component)**
    * 상태 값만 들고 있거나, 자기 자신의 값만 이용해 아주 가벼운 순수 함수만 수행합니다. 가독성을 위해 정적 팩토리 메서드 사용을 권장합니다.
    * 틱 루프 성능을 위해 가변 객체도 적극 허용합니다.
2. **코어 시스템 객체 (Core System / Rule Object)**
    * 게임의 근간이 되는 거대한 물리적 규칙이나 수학적 뼈대를 가집니다. 그 자체가 하나의 하위 시스템이므로 명시적인 생성자로 생성합니다.
3. **관리자 및 시스템 (Manager / System)**
    * 데이터와 코어 객체들을 조립하여 게임의 시간과 상태 흐름을 전역적으로 통제합니다. 복잡한 비즈니스 로직이 구현되는 곳입니다.
4. **입력 컨트롤러 (Input Controller / Command)**
    * 유저의 입력을 감지하는 가장 바깥쪽 껍데기입니다. 비즈니스 로직을 직접 처리하지 않고 관리자에게 제어권을 토스합니다.

### 2.2. Engine-Native
* 외부 기술로부터 도메인을 격리하려는 시도를 지양합니다.
* Bukkit 엔진 객체를 로직 내 1급 객체로 직접 수용하여 복잡도를 낮춥니다.

### 2.3. Component-Based OOP
* 상속 트리를 깊게 구성하지 않고 컴포넌트 기반 조립 패턴을 채택합니다.
* 깡통 엔티티에 부품을 조립하여 동적으로 능력을 부여합니다.

### 2.4. Manager & State Machine
* 데이터의 흐름은 복잡한 서비스 계층이 아닌 중앙 통제 방식으로 관리합니다.
* 게임의 전체 흐름을 통제하는 State Machine을 활용합니다.
* 각 서브시스템 관리 및 엔진 이벤트와의 직접 연결을 담당하는 Manager를 사용합니다.

---

## 3. Practical Clean Code
게임 개발 특성에 맞춰 클린 코드를 실용적으로 적용합니다.

### 3.1. Naming Conventions
*   **패키지:** 단수형 소문자 사용 및 엔진 내장 객체와의 충돌 방지 (`player` ❌ → `participant` ⭕)
*   **클래스:** 역할에 따른 **기능적 접미사** 사용 강제 (`~Manager`, `~Component`, `~State`)
    *   의미 없는 찌꺼기 단어(`~Data`, `~Info`, `~Impl`) 사용은 절대 금지합니다.
*   **메서드:** `동사+명사` 형태로 영어 문장처럼 읽히게 작성하며, 과도한 줄임말을 금지합니다. (`calculateDamage` ⭕, `calcDmg` ❌)

### 3.2. Commenting Rules
빠른 이터레이션을 방해하는 불필요한 주석을 배제합니다.
1.  **Self-Documenting 최우선:** '무엇을' 하는지 설명하는 주석 대신 명확한 명명으로 의도를 전달합니다.
2.  **'Why'와 'TODO'만 허용:** 물리 연산 트릭이나 엔진 버그 회피 등 코드만으로 알 수 없는 **숨겨진 기술적 제약**만 주석으로 남깁니다.
3.  **JavaDoc 지연 작성:** 코어 API나 Manager를 제외한 일반 로직의 JavaDoc 작성은 아키텍처가 안정화되는 Post-MVP 단계까지 보류합니다.

---

## 4. The Priority Pyramid
설계 원칙과 현실이 충돌할 때 타협 없이 즉각적으로 의사결정을 내리기 위한 절대 기준입니다.

1.  **👑 1순위: MVP와 생존**
    *   오늘 당장 게임을 실행하여 재미를 검증할 수 있는 '동작하는 코드' 확보를 최우선으로 합니다.
2.  **🥈 2순위: 엔진의 법칙**
    *   클린 코드를 위한 구조화보다 마인크래프트 서버 틱 유지를 우선하며, 필요한 경우 `onTick` 루프 내에서의 절차적 하드코딩을 허용합니다.
3.  **🥉 3순위: GDD 기반 가독성**
    *   디자인 패턴 준수보다 기획 의도의 명확한 전달을 우선합니다. (파일 개수보다 이름의 의도 전달력이 중요)
4.  **🔨 4순위: 구현 패턴 준수**
    *   패턴은 개발을 돕는 도구일 뿐입니다. 오버엔지니어링이 예상되는 단순 로직에는 패턴을 강제하지 않습니다.

---

## 5. Development Pipeline
성급한 최적화와 오버엔지니어링을 막기 위해, 모든 기능 개발은 아래의 파이프라인 순서를 엄격하게 지켜서 진행합니다. 하위 단계가 완료되지 않은 상태에서 상위 단계를 진행하는 것을 금지합니다.

1. **MVP (Core Loop):** 가장 원시적인 형태(명령어 등)로 게임의 시작-전투-종료 루프를 뚫어 재미를 검증합니다.
2. **UX Alignment (흐름 연결):** 파편화된 로직(명령어)을 유저 친화적인 조작(블록 우클릭, 엔티티 상호작용 등)으로 연결합니다.
3. **Feature Expansion (기획 구현):** 스킬, 자본, 상점 등 세부 비즈니스 기획을 덧붙입니다.
4. **Polishing (디테일):** 파티클 궤적, 사운드, 스코어보드 너비 고정 등 시각적/청각적 디테일을 다듬습니다.
5. **Optimization (최적화):** 모든 기능 개발이 완료된 후, 프로파일링을 통해 병목 구간을 찾고 복잡한 디자인 패턴 적용 및 코드 리팩토링을 진행합니다.
