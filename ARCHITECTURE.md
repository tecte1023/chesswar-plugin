# 🏗️ ChessWar Architecture & Guidelines

## 1. Architectural Background
본 프로젝트는 1년간의 개발 과정을 거치며 아키텍처의 근본적인 전환, 즉 `Soft Reset`을 단행했습니다. 
초기에는 엔터프라이즈 환경의 DDD 및 헥사고날 아키텍처를 채택하였으나, 마인크래프트의 물리적 틱 엔진 환경에서는 이러한 추상화가 과도한 매핑 오버헤드와 개발 속도 저하를 유발한다는 것을 확인했습니다.

성능 최적화를 위한 DOD 및 순수 ECS 도입 역시 고려하였으나, Java 객체의 메모리 파편화 특성상 캐시 최적화의 이점을 얻기 어렵다고 판단하여 기각했습니다.

**[최종 채택 아키텍처]**
결론적으로 본 프로젝트는 기획 중심의 GDD 유비쿼터스 언어를 수용하고, 전통적 상속의 한계를 극복하기 위해 `Component-Based OOP` 패턴을 채택했습니다. 이는 엔진 환경에서 가장 빠르고 유연하게 MVP를 검증하기 위한 최적의 엔지니어링 결정입니다.

---

## 2. Core Patterns
본 프로젝트는 아래의 핵심 패턴을 기반으로 코드를 작성합니다.

### 2.1. Object Roles
모든 객체는 프로젝트 내 역할에 따라 아래 4가지 카테고리로 명확히 분류합니다. 이는 백엔드의 관성을 버리고 게임 개발에 최적화된 설계를 유지하기 위함입니다.

1. **[순수 데이터 객체] (State / Component)**
    * `Struct` 또는 `Component`의 성격을 가지며 상태 값만 들고 있습니다. **어떠한 비즈니스 로직이나 상태 전이 규칙(예: reset)도 포함하지 않는 완전한 깡통 객체여야 합니다.**
    * **[Cold Path: Record 사용]**: 생성 빈도가 낮고 단순 설정값(Configuration)이 목적인 경우 Java `record`를 사용합니다.
    * **[Session Path: Class 사용]**: 매치 또는 세션 단위로 유지되는 가변 상태는 `final class`를 사용합니다. 초기화 시에는 매니저에 의한 **객체 재할당(Reallocation)**을 원칙으로 하여 데이터 오염을 방지합니다.
    * **[Hot Path: Class 사용]**: 틱 루프 내에서 빈번하게 생성되는 핵심 부품은 `final class`와 Flyweight 패턴을 사용합니다. GC 부담을 제로로 만들기 위해 매니저에 의한 **가변 조작(Mutation)**을 허용합니다.
2. **[코어 시스템 객체] (Rule / Physics)**
    * `Core System` 또는 `Rule Object`로서 게임의 근간이 되는 거대한 물리적 규칙이나 수학적 뼈대를 가집니다.
    * **[Knowledge Inclusion]**: 순수 데이터 객체와 달리, 해당 도메인의 수학적 계산이나 물리 변환 로직(예: ChessBoard의 좌표 변환)을 내부에 포함할 수 있습니다. 이는 비즈니스 로직이 아닌 '법칙'으로 간주합니다.
3. **[관리자 및 시스템]**
    * `Manager` 또는 `System`으로서 데이터와 코어 객체들을 조립하여 게임의 시간과 상태 흐름을 전역적으로 통제합니다. 복잡한 비즈니스 로직이 구현되는 곳입니다.
    * **[Engine-Native Feedback]**: 비즈니스 로직 결과에 따른 모든 피드백 출력을 직접 담당하여 프레젠테이션 레이어 분리 오버헤드를 제거합니다.
4. **[입력 컨트롤러]**
    * `Command`, `Listener`, 또는 `GUI`로서 유저의 입력을 감지하는 가장 바깥쪽 껍데기 역할을 수행합니다. 비즈니스 로직을 직접 처리하지 않고 관리자에게 제어권을 토스합니다.
    * **[Engine Physics Control]**: 마인크래프트 물리 엔진 또는 인벤토리 클릭 등의 기술적 접점을 처리하며 엔진 고유의 현상을 방어하는 책임만 가집니다.
    * **[Logic Delegation]**: 데이터를 직접 가공하지 않으며 해당 로직을 소유한 매니저의 식별 메서드를 호출하여 판단을 위임합니다. 특히 `TriumphGui`와 같은 외부 라이브러리 사용 시에도 모든 클릭 핸들러는 매니저의 메서드 호출로 즉시 연결되어야 합니다.

### 2.2. Engine-Native
* 외부 기술로부터 도메인을 격리하려는 시도를 지양합니다.
* Bukkit 엔진 객체를 로직 내 1급 객체로 직접 수용하여 복잡도를 낮춥니다.

### 2.3. Component-Based OOP
* 상속 트리를 깊게 구성하지 않고 컴포넌트 기반 조립 패턴을 채택합니다.
* 깡통 엔티티에 부품을 조립하여 동적으로 능력을 부여합니다.

### 2.4. Manager & State Control
* 데이터의 흐름은 복잡한 서비스 계층이 아닌 중앙 통제 방식으로 관리합니다.
* 게임의 흐름은 데이터와 로직이 분리된 `FSM` 구조를 통해 명시적으로 통제합니다.
* 각 서브시스템 관리 및 엔진 이벤트와의 직접 연결을 담당하는 `Manager`를 사용합니다.
* **[State-Manager Decoupling]**: 상태 객체 내부에 비즈니스 로직을 포함하여 다형성으로 제어하는 고전적 상태 패턴을 금지합니다. 게임의 진행 상태는 가변성을 허용한 순수 데이터 객체로 격리하여 상위로 끌어올립니다. 매니저는 이 데이터 객체를 단방향 생성자로 주입받거나 메서드 파라미터로 하향 전달받아 로직을 처리하며 매니저와 상태 간의 양방향 순환 참조를 원천 차단합니다.
    * **결정 근거**: 상태 객체가 매니저를 역참조하는 순간 의존성 그래프의 복잡도가 지수적으로 상승하며 틱 엔진 환경에서 런타임 순환 참조 에러의 주원인이 됩니다. 데이터와 로직을 분리하여 단방향 흐름을 강제함으로써 시스템 안정성을 확보하고 불필요한 참조 추적 비용을 제거합니다.
* **[초기화 전략]**: 상태의 초기화는 상태 객체 내부의 `reset()` 메서드가 아닌, 매니저가 새로운 상태 객체를 생성하여 교체하는 **객체 재할당** 방식을 지향합니다. 단, 틱 루프 내의 Hot Path 객체에 한하여 성능을 위해 가변 조작을 허용합니다.
* **[매니저 간 통신 규약]**: 엔진 틱 방어와 도메인 격리를 위해 통신 목적별로 패턴을 엄격히 구분하여 적용합니다.
    * **[Polling]**: 타이머 차감이나 UI 업데이트와 같이 매 틱 지속 발생하는 연속적 갱신에 사용하며 가비지 컬렉터 부하를 최소화합니다.
    * **[Event-Driven]**: 게임 리셋이나 페이즈 전이와 같이 특정 시점에 일회성으로 발생하는 이산적 사건에 사용하며 매니저 간 직접 참조를 차단합니다.
    * **[Top-Down Call]**: 기물 스폰과 같이 틱 지연 없이 원자적 실행이 보장되어야 하는 코어 제어에 한하여 상위 매니저의 하향식 직접 호출을 허용합니다.
        * **결정 근거**: 완벽한 의존성 분리를 위해 커맨드 큐 도입이 가능하나 인프라 정립 시점에서는 실이 큰 오버엔지니어링으로 판단하여 기각합니다. 개발 속도를 우선 확보하고 훗날 최적화 단계에서 무거운 연산에 한정하여 부분적으로 대체할 기술 부채로 규정합니다.

### 2.5. Presentation-Domain Separation
* **프레젠테이션 위임 집중화**: 비즈니스 로직은 화면 출력, 사운드, 텍스트 메시지 등의 프레젠테이션 처리 책임을 직접 가지지 않습니다. 모든 UI 출력은 지정된 단일 프레젠테이션 위임 객체를 거쳐 통제됩니다.
* **White-boxing**: 리소스팩 에셋의 부재로 인해 로직 구현이 지연되는 것을 엄격히 금지합니다. 기능 구현 단계에서는 미구현 UI를 단순 텍스트 기반의 더미로 대체하여 비즈니스 로직의 무결성 검증을 최우선으로 완료합니다.
* **Late Polishing**: 커스텀 폰트, 유니코드 UI, 세부 파티클 연출 등 에셋 의존적인 작업은 메인 루프의 버그가 완전히 소거된 폴리싱 단계에서 프레젠테이션 객체의 내부 포맷을 교체하는 방식으로 일괄 처리합니다.

### 2.6. Performance & Engine Defense
* **[Bunker System]**: 마인크래프트의 엔티티 스폰 및 데이터 로딩은 메인 틱에 부하를 주는 무거운 작업입니다. 이를 페이즈 전이 시점에 실시간으로 처리할 경우 서버 랙(Lag)과 시각적 글리치가 발생하므로, 모든 엔티티를 미리 생성하여 격리된 공간(Bunker)에 대기시킨 후 텔레포트만으로 배치하는 방어적 최적화 기법을 사용합니다.
* **[PDC Snapshot & Restore]**: 엔티티의 휘발성 데이터(체력 등)는 엔진의 청크 언로드 시 손실될 위험이 있습니다. 이를 방지하기 위해 `Persistent Data Container`를 데이터 스냅샷 저장소로 활용하여 엔진의 상태 변화와 관계없이 데이터의 무결성을 보장합니다.
* **[Event-Based Damage Control]**: 바닐라의 무적 상태(`setInvulnerable`)는 물리 엔진의 일부 연산을 차단하지만, 세밀한 조건별 데미지 제어가 어렵고 예기치 않은 사이드 이펙트를 유발할 수 있습니다. 본 프로젝트는 모든 `EntityDamageEvent`를 리스너에서 직접 가로채어 상황(팀원 간 공격, 대기 단계 등)에 따라 `setCancelled(true)` 처리함으로써, 바닐라 물리 법칙을 배제하고 커스텀 전투 로직만을 원자적으로 실행합니다.

---

## 3. Packaging Strategy
본 프로젝트는 빠른 개발 속도와 높은 응집도를 위해 `Feature-Based` + `Role-Subpackaging` 기법을 사용합니다.

### 3.1. 최상위 패키지
* GDD의 핵심 시스템 명칭을 최상위 패키지 단위로 사용합니다.
* 특정 기능을 수정할 때 관련 코드가 하나의 패키지 내에 모여 있어야 합니다.

### 3.2. 하위 패키지
* 각 Feature 패키지 내부에서는 4가지 객체 역할 클래스들이 Flat 구조로 존재합니다.
* **[지연 분리 원칙]**: 파일 이름의 접미사로 역할을 식별할 수 있으므로 초기에는 폴더를 나누지 않습니다. 
* **[분리 임계치]**: 특정 역할의 파일이 **5개 이상** 모여 가독성을 해치거나 명확한 다형성 객체군이 생길 때만 하위 폴더를 생성하여 격리합니다. 그 전까지는 Flat 구조를 유지하여 인지 오버헤드를 최소화합니다.

### 3.3. 의존성 관리 전략
* **Bukkit/Paper API**: 1급 객체로 직접 수용하며 매핑 오버헤드 없이 로직 내에서 직접 사용합니다.
* **[단방향 생성자 주입 원칙]**: 코어 매니저 간의 시스템 의존성은 서버 구동 시점에 `Constructor Injection`을 통해서만 단방향으로 고정합니다. 전역 `Service Locator` 패턴은 가비지 컬렉터 부하와 구조적 결합도를 높이므로 엄격히 금지합니다.
* **외부 라이브러리 및 플러그인**: 변동성이 높은 외부 의존성은 반드시 해당 기능을 담당하는 Feature 패키지 내에서 격리하여 사용합니다.

### 3.4. 관리자 명령어 예외 규정
* **[관리자 명령어의 특수성]**: 시스템 장애 복구 및 강제 개입을 목적으로 하므로 일반적인 의존성 최소화 원칙의 예외를 적용합니다.
* **[다중 의존성 허용]**: 문제를 해결하기 위해 게임 내 모든 서브시스템 매니저를 동시에 주입받는 것을 허용합니다.
* **[물리적 격리]**: 다른 도메인 로직의 아키텍처 오염을 방지하기 위해 반드시 `admin` 패키지에 격리하여 관리합니다.

---

## 4. Practical Clean Code
게임 개발 특성에 맞춰 클린 코드를 실용적으로 적용합니다.

### 4.1. Naming Conventions
* **패키지**: 단수형 소문자 사용 및 엔진 내장 객체와의 충돌 방지를 최우선으로 합니다.
* **클래스**: 역할에 따른 기능적 접미사 사용을 권장합니다.
    * **도메인 핵심 객체**: 프로젝트의 주인공이 되는 데이터 객체는 가독성을 위해 접미사를 생략할 수 있습니다.
    * **의미 없는 찌꺼기 단어**: 시스템의 역할을 설명하지 못하는 모호한 접미사 사용은 절대 금지합니다.
* **메서드**: `동사+명사` 형태를 원칙으로 하되 `@Accessors(fluent = true)`를 활용한 Fluent API 스타일을 허용합니다.
* **줄임말 규칙**: 의미를 알 수 없는 과도한 줄임말은 금지하나 도메인 유비쿼터스 언어는 고유 명사로 간주하여 사용을 허용합니다.

### 4.2. Commenting Rules
빠른 이터레이션을 방해하는 불필요한 주석을 배제합니다.
1. **[Self-Documenting 최우선]**: '무엇을' 하는지 설명하는 주석 대신 명확한 명명으로 의도를 전달합니다.
2. **['Why'와 'TODO'만 허용]**: 코드만으로 알 수 없는 숨겨진 기술적 제약만 주석으로 남깁니다.
3. **[JavaDoc 지연 작성]**: 일반 로직의 JavaDoc 작성은 아키텍처가 안정화되는 단계까지 보류합니다.

### 4.3. Extract Variable & Constant
틱 루프 방어와 가비지 컬렉터 부하 최소화를 위해 변수 및 상수 추출 원칙을 변형 적용합니다.

* **[상수 추출]**: 런타임에 값이 변하지 않으나 객체 생성 비용이 존재하는 데이터는 반드시 `static final` 상수로 추출하여 캐싱합니다.
* **[Flyweight Pattern (할당 강제)]**: `Coordinate`와 같이 도메인 범위가 제한된 객체는 반드시 내부 `static` 배열에 모든 경우의 수를 캐싱합니다. 이때, 개발자의 실수를 방지하기 위해 **기본 생성자를 반드시 `private`으로 닫아야 하며**, 정적 팩토리 메서드(`of`)를 통해서만 캐시된 객체를 반환하도록 설계하여 힙 메모리 할당 비용을 물리적으로 0(Zero-cost)으로 고정해야 합니다.
* **[변수 추출]**: 반복 사용되는 객체 참조를 지역 변수로 추출하여 가독성을 높입니다. 단 변수 분리만을 목적으로 `new` 키워드를 중복 호출하는 행위는 엄격히 금지합니다.

### 4.4. Null Safety & Layered Defense Standards
마인크래프트 틱 엔진의 비트랜잭션 특성과 실시간 성능을 고려하여, 시스템 계층별로 차별화된 Null 방어 전략을 적용합니다.

**[결정 근거]**: 마인크래프트 엔진은 원자적 트랜잭션 롤백을 지원하지 않으므로, 로직 중간에 Lombok `@NonNull`에 의한 무차별적인 런타임 NPE가 발생할 경우 데이터 불일치(예: 돈만 차감되고 아이템 미지급)가 발생할 위험이 큼. 따라서 시스템 입구(Boundary)에서 완벽한 검증과 보상 트랜잭션을 수행하고, 내부 도메인(Core)은 정적 분석에 의존하도록 설계하여 틱 성능과 데이터 무결성을 동시에 확보함.

1. **[Boundary Defense: 외부 경계]**
    * **대상**: `Listener`, `Command`, `Packet Handler` 등 엔진 및 유저 입력과 직접 맞닿는 계층.
    * **전략**: Lombok `@NonNull` 사용을 금지하고, **명시적 `if (null)` 체크와 상태 복구(Rollback)**를 수행합니다.
    * **원칙**: 모든 입력값은 로직 진입 전 완벽하게 검증되어야 하며, 실패 시 유저에게 피드백을 제공하고 안전하게 로직을 종료합니다.

2. **[Core Domain: 내부 핵심]**
    * **대상**: `BoardState`, `Coordinate`, `ChessBoard`, `Piece`, `Participant` 등 검증된 데이터만 다루는 핵심 도메인 객체.
    * **전략**: **JetBrains `@NotNull`과 `@Nullable`을 통한 정적 분석**에 의존합니다.
    * **원칙**: 틱 단위로 수만 번 호출되는 핫 패스(Hot Path)에서 런타임 Null 체크 오버헤드를 제거합니다. 개발자의 실수는 CI/CD 및 IDE 정적 분석 단계에서 차단하는 것을 목표로 합니다.

3. **[Zero-cost Stability]**
    * **Optional Ban**: 리턴 타입 및 필드에서 `Optional` 사용을 전면 금지합니다.
    * **Compact Constructor**: Java `record` 필드 검증은 컴팩트 생성자를 통해 수행합니다.
    * **Exception Management**: 시스템 불변성이 깨지는 치명적 상황에 한해서만 예외를 허용하며, 중앙 처리기에서 관리합니다.

### 4.5. High-Performance Data Structures
틱 루프 내 빈번한 조회가 발생하는 공간 데이터는 탐색 비용을 최소화하기 위해 원시 데이터 구조를 우선합니다.

1. **[2D Array Mapping]**: 체스판과 같이 고정된 크기의 2D 그리드 데이터는 `Map<Coordinate, V>` 대신 `V[][]` 배열을 사용합니다.
    * **결정 근거**: 해시 계산 및 객체 비교 오버헤드를 제거하고 인덱스 참조만으로 O(1) 성능을 보장합니다.
2. **[Flyweight Integration]**: 2D 배열의 인덱스는 Flyweight 패턴이 적용된 `Coordinate`의 x, y 값과 1:1로 대응되도록 설계하여 변환 비용을 최소화합니다.

---

## 5. The Priority Pyramid
설계 원칙과 현실이 충돌할 때 즉각적으로 의사결정을 내리기 위한 절대 기준입니다.

1. **👑 1순위: MVP와 생존**
    * 동작하는 코드 확보를 최우선으로 합니다.
    * **[단서]**: 파이프라인 3단계 이상에 진입한 기능은 기존의 임시 코드를 정규 패턴으로 고도화해야 합니다.
2. **🥈 2순위: 엔진의 법칙**
    * 마인크래프트 서버 틱 유지를 우선하며 필요한 경우 틱 루프 내에서의 절차적 하드코딩을 허용합니다.
3. **🥉 3순위: GDD 기반 가독성**
    * 디자인 패턴 준수보다 기획 의도의 명확한 전달을 우선합니다.
4. **🔨 4순위: 구현 패턴 준수**
    * 패턴은 개발을 돕는 도구일 뿐이므로 오버엔지니어링이 예상되는 단순 로직에는 패턴을 강제하지 않습니다.

---

## 6. Development Pipeline
모든 기능 개발은 아래의 파이프라인 순서를 엄격하게 지켜서 진행합니다.

1. **MVP**: 게임의 시작-전투-종료 루프를 뚫어 재미를 검증합니다.
2. **UX Alignment**: 파편화된 로직을 유저 친화적인 조작으로 연결합니다.
3. **Infrastructure Refactoring**: 기술 부채를 정리합니다. 특히 `Service Locator` 부채를 단방향 생성자 주입 및 하위 시스템 파라미터 제어 구조로 전면 전환합니다.
4. **Feature Expansion**: 세부 비즈니스 기획을 덧붙입니다. 에셋 부재 시 텍스트 기반의 더미 UI를 활용하여 로직 검증을 우선합니다.
5. **Polishing**: 시각적/청각적 디테일을 다듬습니다. 비즈니스 로직이 확정된 후 리소스팩 및 커스텀 UI를 프레젠테이션 객체에 일괄 적용합니다.
6. **Optimization**: 프로파일링을 통해 병목 구간을 식별하고 코드 고도화를 진행합니다.
